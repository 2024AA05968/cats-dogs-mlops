# --- Core utilities (lightweight, safe to install early) ---
numpy==1.26.4
pillow==10.4.0
python-dotenv==1.0.1

# --- Dev & testing ---
pytest==8.3.3
pytest-cov==5.0.0

# --- Tracking & data versioning (to be installed later) ---
mlflow==2.16.0
dvc==3.56.0
dvc-gdrive==3.0.1  # optional, for remote if you choose Google Drive later

# --- API for inference (we'll use in M2) ---
fastapi==0.115.4
uvicorn[standard]==0.32.0

# --- Choose ONE framework later (we'll decide together before installing) ---
# torch==2.4.1
# torchvision==0.19.1
# OR
# tensorflow==2.16.2